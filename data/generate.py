"""Scripted LLM generation pass for the three firm corpora.

Schema first, then generate: the record shapes in ``data/README.md`` are fixed before
this runs, and the R4 bio wording is hand-tuned afterwards rather than regenerated.
"""
