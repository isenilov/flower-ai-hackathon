[project]
name = "consortium"
version = "0.2.0"
description = "A collaborative disclosure harness for agents that cannot pool their data"
readme = "README.md"
# A table with a file, not an SPDX string: fab-format-version 1 requires the file form.
license = { file = "LICENSE" }
requires-python = ">=3.13"
dependencies = [
    "flwr[simulation]>=1.34.0",
    "numpy>=2.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["backend", "agents"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
# Ruff formats fenced Python in Markdown. The brief's code blocks are illustration, not
# code, and reformatting them churns a doc four people are reading.
extend-exclude = ["*.md"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.flwr.app]
# Must equal the Flower account name exactly, and the app name below cannot change after
# the first publication. Published as @i53n1/consortium.
publisher = "i53n1"
fab-format-version = 1
flwr-version-target = "1.35.0"
# Hub keeps only .py/.toml/.md/.yaml/.json/.jsonl and LICENSE, and once any user rule is
# set every surviving file must match that list — hence .gitignore here. The rest is
# excluded so the published app is the harness, not our desk.
fab-exclude = [
    ".gitignore",
    "CLAUDE.md",
    ".claude/**",
    "docs/**",
    "frontend/**",
    "tests/**",
]

# Hub rejects a mixed bundle — 422 "must contain either only 'agentapp' or exactly
# 'serverapp' and 'clientapp'". The local CLI validates all three happily, so this only
# surfaces at publish time. `serverapp` / `clientapp` stay in the repo as modules; the
# federated surface runs via `flwr.simulation.run_simulation`, not `flwr run .`.
[tool.flwr.app.components]
agentapp = "backend.agent_app:app"

[tool.flwr.app.config]
num-rounds = 3
num-firms = 3
tau = 0.6
section-f-cap = 10
# Set to a model the target SuperGrid serves to get a narrated report; empty prints the
# coverage table only, so the harness still runs where no model endpoint is configured.
model = ""

[tool.flwr.app.config.agent]
input = ""


#######################################################################
# CONFIGURATION MIGRATION NOTICE:
#
# What was previously called "federation config" for SuperLink
# connections in pyproject.toml has been renamed and moved.
#
# These settings are now **SuperLink connection configuration**
# and are defined in the Flower configuration file.
#
# The entries below are commented out intentionally and are kept
# only as a migration reference.
#
# Docs: https://flower.ai/docs/framework/ref-flower-configuration.html
#######################################################################

# [tool.flwr.federations]
# default = "local-sim"

# [tool.flwr.federations.local-sim]
# options.num-supernodes = 3
